# Copy this file to .env, .env.local or .env.production as needed
# Values here are read by Vite at build time (VITE_*)

# Optional base URL for link generation (UI uses Router basename automatically)
# VITE_BASE_URL=""

# Direct APK URL (recommended: GitHub Release asset)
# e.g. https://github.com/<owner>/<repo>/releases/download/<tag>/app-1.0.0.apk
VITE_ANDROID_APK_URL=

# iOS enterprise manifest (itms-services). Leave empty if using TestFlight/App Store.
# e.g. https://your-domain/downloads/ios/manifest.plist
VITE_IOS_ENTERPRISE_PLIST_URL=

# TestFlight public link (if used)
VITE_TESTFLIGHT_URL=https://testflight.apple.com/

# Note: Les témoignages sont statiques dans le code, aucune variable requise ici.

# Formulaires — configurez des endpoints externes (Formspree, Getform, Formspark, etc.)
# Laissez vide pour utiliser le fallback (mailto pour Contact, mailto/UX pour Recrutement).
# Doivent accepter un POST multipart/form-data (nom, email, phone, subject, message, etc.).
VITE_CONTACT_FORM_ENDPOINT=
VITE_RECRUITMENT_FORM_ENDPOINT=
